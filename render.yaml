services:
  - type: web
    name: jiten
    env: node
    plan: free
    branch: main
    buildCommand: npm install && npm install --prefix backend && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
