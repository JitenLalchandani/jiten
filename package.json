{
  "name": "web-development",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "backend": "npm install --prefix backend && node backend/server.js",
    "build": "npm install --prefix backend && vite build",
    "start": "node backend/server.js",
    "preview": "vite preview"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=10.0.0"
  },
  "postinstall": "npm install --prefix backend",
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0",
    "lucide-react": "^0.470.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "concurrently": "^10.0.3",
    "vite": "^5.4.1"
  }
}
